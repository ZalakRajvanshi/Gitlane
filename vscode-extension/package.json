{
  "name": "gitlane",
  "displayName": "Gitlane — AI Commits & Secret Guard",
  "description": "AI commit messages (Conventional Commits) + automatic secret detection and auto-fix to .env. One-click commit, push, and GitHub repo creation. A free, local Copilot alternative for commit messages.",
  "version": "0.4.1",
  "publisher": "zalakrajvanshi",
  "license": "MIT",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/ZalakRajvanshi/gitlane.git"
  },
  "homepage": "https://github.com/ZalakRajvanshi/gitlane#readme",
  "bugs": { "url": "https://github.com/ZalakRajvanshi/gitlane/issues" },
  "engines": { "vscode": "^1.85.0" },
  "categories": ["SCM Providers", "AI", "Machine Learning"],
  "keywords": [
    "gitlane",
    "ai",
    "ai commit",
    "ai commit message",
    "commit message",
    "commit message generator",
    "conventional commits",
    "smart commit",
    "git ai",
    "git assistant",
    "secret scanner",
    "secret detection",
    "credential scanner",
    "gitleaks",
    "detect secrets",
    "leaked api key",
    "env",
    "dotenv",
    "one click commit",
    "copilot alternative",
    "groq",
    "llama",
    "github",
    "productivity",
    "streak"
  ],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      { "command": "gitlane.commitNow",              "title": "Gitlane: Commit Now (AI message + secret scan + push)" },
      { "command": "gitlane.generateCommitMessage",  "title": "Gitlane: Generate AI Commit Message",                  "icon": "$(sparkle)" },
      { "command": "gitlane.ask",                    "title": "Gitlane: Ask a Question About My Work" },
      { "command": "gitlane.openDashboard",          "title": "Gitlane: Open Dashboard (Browser)" },
      { "command": "gitlane.showMenu",               "title": "Gitlane: Show Menu" }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "gitlane.generateCommitMessage",
          "group": "navigation",
          "when": "scmProvider == git"
        }
      ]
    },
    "keybindings": [
      {
        "command": "gitlane.generateCommitMessage",
        "key":     "ctrl+alt+m",
        "mac":     "cmd+alt+m"
      }
    ],
    "configuration": {
      "title": "Gitlane",
      "properties": {
        "gitlane.projectRoot": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the Gitlane project folder (the one containing data/gitmind.db and .env). Asked on first run."
        },
        "gitlane.dashboardUrl": {
          "type": "string",
          "default": "http://localhost:7123",
          "description": "Where the optional Python browser dashboard runs (only used by the 'Open Dashboard' command)."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch":   "tsc -p . -w"
  },
  "dependencies": {
    "sql.js": "^1.10.3"
  },
  "devDependencies": {
    "@types/node":   "^20.11.0",
    "@types/sql.js": "^1.4.9",
    "@types/vscode": "^1.85.0",
    "typescript":    "^5.4.0"
  }
}
